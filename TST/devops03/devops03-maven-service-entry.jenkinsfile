// 最简实现 - 使用 Extended Choice Parameter
def ALL_JOBS = [
    'TST/devops03/devops03-maven-service-dockercompose',
    'TST/devops03/devops03-maven-service',
    'TST/devops03/devops03-maven-service-standard'
]

pipeline {
    agent any
    
    parameters {
        // Extended Choice Parameter - 多选参数
        extendedChoice(
            name: 'DOWNSTREAM_JOBS',
            type: 'PT_CHECKBOX',
            value: ALL_JOBS.join(','),
            defaultValue: ALL_JOBS.join(','),  // 这里设置默认全选
            description: '选择要触发的下游任务',
            multiSelectDelimiter: ',',
            visibleItemCount: 5
        )
    }
    
    stages {
        stage('触发下游') {
            steps {
                script {
                    echo "🚀 开始触发下游任务"
                    
                    // 处理选中的任务
                    def selectedJobs = params.DOWNSTREAM_JOBS.split(',').collect { it.trim() }
                    
                    echo "已选择 ${selectedJobs.size()} 个任务："
                    selectedJobs.each { job ->
                        echo "  • ${job}"
                    }
                    
                    // 并行触发
                    parallel selectedJobs.collectEntries { jobName ->
                        ["触发 ${jobName}": {
                            echo "触发: ${jobName}"
                            build job: jobName, wait: false
                        }]
                    }
                    
                    echo "✅ 所有任务已触发"
                }
            }
        }
    }
}